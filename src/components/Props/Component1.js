import React from 'react';

const Component1 = (props) => {
    return (
        <div>
            <h1>Salom {props.name}</h1>
            <h1>Mening yoshim {props.age}</h1>
        </div>
    );
};

export default Component1;